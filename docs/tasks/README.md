# Task Notes

These task files are historical planning records from earlier Monknot/Markprev development passes. They are useful for context, but they are not the active source of truth for current implementation state.

Use current sources for authoritative status:

- `AGENTS.md` for the current architecture, target layout, and test commands.
- `improvement_progress.md` for completed slices and remaining blockers.
- `Package.swift`, `script/build_and_run.sh`, and the current tests for build/test truth.

Before acting on any task here, re-check the referenced files in the current worktree. Older references to `Markprev*`, `Tests/*/main.swift`, orphaned smoke tests, or undeclared SwiftPM targets may have already been superseded.
